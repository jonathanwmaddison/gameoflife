import React from 'react'

function GenerationDisplay(props) {
    const { generation } = props;
    return (
         <div> Generation:  {generation} </div>
    )
}

export default GenerationDisplay;
